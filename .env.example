# Copy this file to .env and set values. Only the project root .env is used (Docker and local).

# ----- Backend -----
APP_NAME="Strategic Survey Engine"
DEBUG=false
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/strategic_survey
DATABASE_ECHO=false
PUBLIC_SCHEMA=public
ADMIN_API_KEY=admin123
JWT_SECRET_KEY=change-me-in-production

# ----- Frontend -----
# Local: /api (Vite proxy). Docker: compose passes http://localhost:8000.
VITE_API_URL=/api
