# Optional: used by docker-compose and/or local runs
# Copy to .env and adjust.

# Admin API key (backend). If set, Admin API requires X-Admin-API-Key header.
# ADMIN_API_KEY=

# Frontend: same value so the UI can call the Admin API when backend requires it.
# VITE_ADMIN_API_KEY=
