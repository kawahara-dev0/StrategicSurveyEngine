# API base URL. Use /api when running behind Vite proxy (dev). Empty = /api.
VITE_API_URL=/api

# Admin API key (optional). If backend ADMIN_API_KEY is set, set the same value here.
# VITE_ADMIN_API_KEY=
