# Copy to .env and adjust (backend/.env)
APP_NAME="Strategic Survey Engine"
DEBUG=false

DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/strategic_survey
DATABASE_ECHO=false

PUBLIC_SCHEMA=public

# Admin API (Phase 2) - set to restrict access. Empty = allow all (dev).
ADMIN_API_KEY=
